# Workspace packages resolve to each other from source, not the registry.
link-workspace-packages=true

# Surface peer problems instead of silently installing a second React.
auto-install-peers=true
strict-peer-dependencies=false
